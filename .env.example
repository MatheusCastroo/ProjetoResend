# Copie para .env e preencha (php carrega via env.php no bootstrap).
# Docker Compose injeta este arquivo no container (sem MySQL embutido — use seu servidor MySQL).
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=projeto_resend
DB_USER=root
DB_PASS=

WEB_PORT=8080
DEFAULT_TEST_EMAIL=destinatario@exemplo.com
